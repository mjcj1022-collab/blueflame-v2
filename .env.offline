# Loaded automatically by `vite build --mode offline` (see package.json's
# build:offline script). Forces the standalone build: no backend calls, no
# paywall gate, local-only sign-in. Not a secret — safe to commit.
VITE_OFFLINE=1
